version: 2

build:
  tools:
    python: "3.9"

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - brotli
        - secure
        - socks

sphinx:
  fail_on_warning: true
