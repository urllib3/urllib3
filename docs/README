# Building the Docs

First install Sphinx:

    pip install sphinx

Install pyopenssl dependencies, to avoid some build errors. (Optional)

    # This step is optional
    pip install ndg-httpsclient pyasn1

Then build: 

    make html
