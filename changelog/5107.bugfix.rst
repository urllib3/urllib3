Fixed a ``ValueError: Context has already been used to create a Connection, it cannot be mutated again`` raised from the ``pyOpenSSL`` backend when an ``HTTPSConnectionPool`` reused the same ``PyOpenSSLContext`` for more than one request. The wrapper now tracks the user's customisations and rebuilds the underlying ``OpenSSL.SSL.Context`` on the second ``wrap_socket`` call from the same pool.
