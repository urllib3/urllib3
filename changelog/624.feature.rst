Add streaming multipart encoding with bounded file reads, rewind support for
retries, and multipart response decoding. ``encode_multipart_formdata()`` uses
the encoder internally while retaining its bytes return value.
