Fixed HTTP/2 connections to surface ``StreamReset`` and ``ConnectionTerminated``
events (and unexpected connection closure) as ``ProtocolError`` with the peer's
HTTP/2 error code, instead of hanging while waiting for a response. A graceful
``GOAWAY`` after a completed stream still returns the response and closes the
connection so it is not reused.
